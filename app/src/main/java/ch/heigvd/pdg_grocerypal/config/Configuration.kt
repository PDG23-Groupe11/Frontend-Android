package ch.heigvd.pdg_grocerypal.config

object Configuration {

    // URL pour le backend local (utilisé dans un émulateur Android)
//     const val BaseURL = "http://10.0.2.2:8080"

    // URL pour le backend en ligne
    const val BaseURL = "http://35.246.127.184:8080"

    // URL pour le backend local
    // const val BaseURL = "http://127.0.0.1:8080"
}